<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eccproductions.networkwidget"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- //MainActivity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- //AboutUsActivity -->
        <activity
            android:name=".AboutUsActivity"
            android:label="@string/title_activity_aboutus"
            android:theme="@android:style/Theme.Dialog" >
            <intent-filter>
                <action android:name="com.eccproductions.networkwidget.ABOUTUS" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- //PreferencesActivity -->
        <activity
            android:name=".PreferencesActivity"
            android:label="@string/title_activity_preferences" >
            <intent-filter>
                <action android:name="com.eccproductions.networkwidget.PREFERENCES" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- //WidgetReciever -->
        <receiver
            android:name=".NetworkWidgetProvider"
            android:icon="@drawable/ic_launcher"
            android:label="@string/title_widget_networkwidget"
            android:permission="android.permission.READ_PHONE_STATE" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>
        <!-- //UpdateWidgetService -->
        <service android:name=".UpdateWidgetService" >
        </service>
    </application>

</manifest>